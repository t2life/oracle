# 作業4-7 次ラウンド是正実装と検証結果（2026-04-22）

## 1. 対象
本書は「是正指示書（次ラウンド）」に基づく作業4〜7の実装・検証結果をまとめる。

優先是正項目:
1. U-12/U-14/U-15/U-16 モバイル本実装
2. HybridStore から完全永続化へ拡張
3. 最小 CI/CD・監査ログ・監視の着手

## 2. 作業4（実装）

### 2-1. Backend API 仕上げ
- 監査ログの共通記録関数を API 層に追加。
- 認証系に監査ログを追加。
  - `/auth/login`
  - `/auth/apple`
  - `/auth/google`
  - `/auth/logout`（新規）
- 課金系に監査ログを追加。
  - `/purchase/verify`
  - `/purchase/restore`
- 管理操作に監査ログを追加。
  - `/admin/login`
  - `/admin/announcements`（POST）
  - `/admin/campaigns`（POST）
  - `/admin/campaigns/{campaign_id}/dispatch`
- 監査ログ参照 API を追加。
  - `GET /admin/audit-logs?limit=...`
- 監視向け詳細ヘルスチェックを追加。
  - `GET /health/ready`

### 2-2. 永続化拡張の運用基盤
- マイグレーション SQL を追加。
  - `scripts/migrations/001_expand_persistence_up.sql`
  - `scripts/migrations/001_expand_persistence_down.sql`
- マイグレーション実行スクリプトを追加。
  - `scripts/run_migration.py`
  - DB未起動時にハングしづらいよう、`connect_timeout=5` を付与。
  - 失敗時は簡潔なエラーメッセージを返す実装に調整。

### 2-3. 本番運用向けガード
- 実行モード設定 `ORACLE_RUNTIME_MODE` を追加（`local/staging/production`）。
- `staging/production` で `ORACLE_PERSISTENCE_BACKEND=inmemory` を禁止。
- `staging/production` では永続化初期化失敗時に InMemory へ自動フォールバックしないよう変更。

### 2-4. CI/CD 最小構成
- GitHub Actions ワークフローを追加。
  - `.github/workflows/backend-ci.yml`
- 内容:
  - Python セットアップ
  - 依存インストール
  - `pytest`
  - `scripts/smoke_test.py`
  - `scripts/init_persistence.py`（hybrid + Postgres/Redis 前提）

## 3. 作業5（テスト）

### 3-1. 追加テスト
- `tests/test_extended_api.py`
  - `GET /health/ready`
  - `POST /auth/logout`
  - `GET /admin/audit-logs`（正常系/認可エラー）
  - 監査ログ action（`auth.login`, `auth.logout`, `billing.purchase_restore`）の回帰確認
- `tests/test_persistence_bootstrap.py`
  - `ORACLE_RUNTIME_MODE` 読み取り確認
  - `staging + inmemory` 拒否動作の確認

### 3-2. スモークテスト拡張
- `scripts/smoke_test.py` に以下を追加。
  - `GET /health/ready`
  - `POST /auth/login`
  - `POST /auth/logout`
  - `GET /admin/audit-logs`

### 3-3. 実行結果
- `pytest -q` : **16 passed**
- `scripts/smoke_test.py` : **成功**
- `scripts/init_persistence.py`（local + inmemory + postgres/redis disabled）: **成功**
- `scripts/init_persistence.py`（staging + inmemory）: **想定通り失敗**（ガードが有効）
- `scripts/run_migration.py up` : **DB未起動のため接続タイムアウト失敗**（想定通り）

## 4. 作業6（最終整合）
- README を更新。
  - `ORACLE_RUNTIME_MODE` の説明追記
  - `scripts/run_migration.py up/down` 手順追記
  - `/health` と `/health/ready` の監視用途追記
- 既存 API 仕様との整合を確認。
  - 追加エンドポイントのレスポンス型は `schemas.py` に反映済み。
- 静的エラー確認（対象ファイル）: 問題なし。

## 5. 作業7（サマリー）
本書をもって作業4〜7のサマリー兼、次アクションへの引き継ぎ資料とする。

## 6. 残課題・次アクション
1. Postgres/Redis 実機環境で `scripts/run_migration.py up/down` の本番同等検証を実施する。
2. `ORACLE_RUNTIME_MODE=staging` + `ORACLE_PERSISTENCE_BACKEND=hybrid` で起動試験を実施する。
3. CI 実行基盤（GitHub リポジトリ）へ workflow を接続し、PR 実行ログを取得する。
4. 監査ログの保持期間・マスキング方針を運用設計に落とし込む。
