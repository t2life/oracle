# リーディングアプリ 作業1-3 調査結果と修正方針

## 1. 作業条件

| 項目 | 内容 |
|---|---|
| 作業日 | 2026-04-22 |
| 作業対象フォルダ | C:\Users\81907\Desktop\オラクルカード\system |
| ドキュメント保存フォルダ | C:\Users\81907\Desktop\オラクルカード\system\docs |
| 作業非対象フォルダ | なし |
| GitHubリポジトリ | なし |
| 実施範囲 | 作業1〜作業3（調査と方針策定のみ） |

## 2. 作業1 事実確認

### 2.1 ripgrep による高速検索結果

以下を対象に、READモードで全文調査した。

- README.md
- オラクルアプリ.code-workspace
- .gitignore
- .vscode/settings.json
- docs/リーディングアプリ開発 詳細仕様書.md
- docs/リーディングアプリ開発 追補仕様書.md
- docs/更なる課題20260417.docx（抽出読み取り）
- docs/工程表ガンチャート.xlsx（抽出読み取り）

確認事実:

- 実装コード（.py、.ts、.js、.dart など）は配置されていない。
- ログファイル（.log、.out、.err、.trace）は存在しない。
- 既存ファイルは仕様書と管理資料が中心で、実装フェーズ前の状態。

### 2.2 /docs の md ファイルで確認した背景

docs/リーディングアプリ開発 詳細仕様書.md から確認した事実:

- アプリは占い単体ではなく、VTuber、物販、鑑定導線を含む事業設計。
- 推奨技術は Flutter、FastAPI または Node.js、PostgreSQL、Redis、Next.js。
- 占い抽選ロジックと演出ロジックの分離が明記されている。
- 通知、課金、分析イベント、管理画面を最初から責務分離する方針。

docs/リーディングアプリ開発 追補仕様書.md から確認した事実:

- UIは1画面1目的、1カラム中心、主導線を占い開始に集中。
- ペイウォールは結果直後、制限到達時、履歴制限時に差し込む方針。
- 通知許諾は価値体験後の提示が推奨されている。

### 2.3 前回作業の痕跡確認

docs/更なる課題20260417.docx を抽出確認し、以下を確認:

- 画面遷移、ワイヤーフレーム、導線最適化の記述が中心。
- エラー記録、障害ログ、例外トレース、修正履歴の明示は確認できない。

docs/工程表ガンチャート.xlsx を抽出確認し、以下を確認:

- 企画、設計、実装、テスト、申請、運用の工程管理資料。
- 不具合票や障害原因の明示ではなく、進行計画の整理が中心。

今回との差分として把握できる事実:

- 今回も実装コード不在のため、前回と同様に実装レベルの不具合比較は不能。
- 前回資料は設計進行の補強であり、コード修正履歴そのものではない。

### 2.4 Git 状態とブランチ状態

調査結果:

- 当該フォルダは git repository ではない。
- ブランチ情報、push状況、コミット履歴は取得不能。

### 2.5 ログファイルの時系列横断

調査結果:

- 横断対象ログファイル自体が存在しない。
- 時系列原因分析は実施不能。

### 2.6 クラス・関数・import・dataclass の網羅確認

調査結果:

- クラス定義: 該当ファイルなし。
- 関数・メソッド定義: 該当ファイルなし。
- import文: 該当ファイルなし。
- dataclass利用: 該当ファイルなし。
- 呼び出し配線: 該当ファイルなし。

### 2.7 テストモード固有か本番共有かの切り分け

調査結果:

- 実装コード、実行設定、ログのいずれも不在。
- テストモード固有問題か本番共有問題かの切り分けは実施不能。

### 2.8 対象外残渣の除外確認

調査結果:

- 本プロジェクト対象外の残渣記述が含まれていたため、以降の実装判断から除外する。
- 実装対象は /docs の占いアプリ仕様（詳細仕様書・追補仕様書）に限定する。

## 3. 作業2 関連ファイル調査（修正コーディングなし）

### 3.1 ファイル間オーケストレーションと責務

現時点で確認できる責務分担:

- 詳細仕様書: 機能要件、非機能要件、アーキテクチャ方針、KPI定義。
- 追補仕様書: 画面遷移、画面項目、UI設計ルール。
- docx/xlsx: 画面案補強と工程計画。
- README / workspace設定: Python環境前提の作業場定義。

未確認事項（理由: 実装不在）:

- モジュール依存関係。
- 関数呼び出し経路。
- 設定値の流用可否。
- 命名規約の実装準拠性。

### 3.2 既存設定流用の観点

確認結果:

- README では Pythonプロジェクト前提。
- オラクルアプリ.code-workspace と .vscode/settings.json で .venv を既定化。
- ただし README は system.code-workspace を参照しており、実ファイル名と不一致。

影響:

- 新規参加者の初期起動手順で混乱が生じる。
- 実装着手前にドキュメント整合を取る必要がある。

### 3.3 業界標準・優良事例との比較

確認結果:

- 占いアプリとしての要件定義は、体験導線、課金導線、通知導線の分離設計ができており、一般的なモバイル運用標準に沿う。
- 一方で、運用健全性に必要な時刻同期、障害復旧、再送制御、監査ログの実装定義はコード上で未確認。

評価:

- 現状は設計妥当性はあるが、運用健全性を評価するための実装が未提示。

### 3.4 コードレベル原因特定に向けた調査可否

調査結果:

- 実装コードが存在しないため、関数単位の原因特定は成立しない。
- コードレベルで特定できる唯一の原因は、検証対象コード未配置である。

## 4. 作業3 修正方針（調査回答）

### 4.1 問題の原因（コードレベル）

原因1:

- 検証対象となる実装コードファイルがワークスペースに存在しない。

原因2:

- Git管理外のため、前回と今回の実装差分やエラー再現差分を追跡できない。

原因3:

- ログファイル不在のため、時系列障害分析が不能。

原因4:

- 対象外残渣が混在しており、実装対象の境界が文書上で一部不明瞭だった。

### 4.2 変更方針（作業4以降の実装前提）

方針1:

- 新規クラス乱立を避け、仕様書が示す責務単位で最小実装する。

方針2:

- 占いロジック、演出ロジック、課金、通知、履歴、外部導線を分離する。

方針3:

- ハードコーディングを避け、テーマ、デッキ、課金商品、通知文言は設定化する。

方針4:

- 時刻処理はJSTで統一し、保存時はタイムゾーン明示を徹底する。

方針5:

- ログは日本語で出力し、debugレベルを段階導入する。

方針6:

- 既存設定値がある場合は再利用し、類似パラメータ乱立を禁止する。

### 4.3 想定する最小変更単位

現時点では実装ファイル不在のため、以下は変更候補の粒度のみ定義する。

- 構成定義ファイル（アプリ設定、環境設定）
- 占いセッション管理（開始、選択、確定）
- 履歴保存と制限制御
- 通知配信判定
- 課金状態判定
- 管理画面向けマスタ管理

### 4.4 他システムへの影響観点

影響評価の対象:

- 課金状態判定の変更は、結果表示、履歴閲覧、深掘り導線に波及。
- 通知判定の変更は、再訪率、継続率、告知運用に波及。
- テーマ/デッキマスタ変更は、UI表示、API応答、分析イベントに波及。
- 時刻処理変更は、履歴整列、通知予約、分析日次集計に波及。

現状判定:

- 実ファイル不在のため、影響範囲は設計レベルでの暫定評価に留まる。

### 4.5 オーバーライド回避と命名統合の方針

方針:

- 一時逃げのオーバーライドは採用しない。
- 既存オーバーライドがある場合は責務別に統合する。
- 命名と配置が混在している場合は、モジュール境界を優先して統合する。

現状:

- 実装ファイル不在のため、統合対象は未確定。

### 4.6 作業4実施前に必要な提供物

以下が未提供のため、作業4（実装修正）開始条件を満たしていない。

- 実装ソース一式（フロントエンド、バックエンド、管理画面）
- 実行設定ファイル（環境変数テンプレート、実行手順）
- テストコードまたは最小再現手順
- エラーログまたは障害発生時刻が分かる運用ログ
- 既存の修正履歴（前回修正内容）

## 5. 重要項目（承認確認）

作業4へ進む前に、以下の可否確認が必要。

1. 実装ソース一式の提供を受けた後、コード調査と修正設計に進行してよいか。
2. Git管理外のまま進めるか、先にリポジトリ初期化を実施するか。
3. ログ不在のため、再現テスト手順を先に整備してから修正に入る進め方でよいか。
4. READMEのワークスペース名不一致修正を、作業4の前提整備として先行実施してよいか。
