# リーディングアプリ 作業1-3 再調査と全体実装方針

## 1. 作業条件

| 項目 | 内容 |
|---|---|
| 作業日 | 2026-04-22 |
| 作業対象フォルダ | C:\Users\81907\Desktop\オラクルカード\system |
| ドキュメント保存フォルダ | C:\Users\81907\Desktop\オラクルカード\system\docs |
| 作業非対象フォルダ | backup |
| GitHubリポジトリ | なし（存在確認のみ） |

## 2. 作業1 事実確認

### 2.1 既存ファイルと仕様資料の確認

READ モードで確認した対象:

- docs/リーディングアプリ開発 詳細仕様書.md
- docs/リーディングアプリ開発 追補仕様書.md
- docs/更なる課題20260417.docx（抽出確認）
- docs/工程表ガンチャート.xlsx（抽出確認）
- docs/既存作業ファイル一式
- src/oracle_app 配下の全実装
- tests, scripts, README, workspace設定

確認結果:

- バックエンドは FastAPI で実装済みだが、仕様全体では未充足。
- モバイルアプリ実装ファイル（.dart）は未存在。
- 管理画面実装ファイル（.tsx 等）は未存在。
- 永続DBマイグレーション（.sql、alembic等）は未存在。

### 2.2 Git とログの確認

確認結果:

- git repository ではない。
- ログファイル（.log/.out/.err/.trace）は未存在。

### 2.3 クラス・関数・配線の確認

確認結果:

- `src/oracle_app` に API・schema・model・service・orchestrator が存在。
- `api.py` の既存エンドポイントは主に占いフロー中心。
- `store.py` は `InMemoryStore` のみで永続化がない。
- `tests` は6件で、占い中心の観点に限定。

### 2.4 画面ID対応の確認

確認結果:

- U-xx / A-xx のUIファイルが未存在。
- API側は U-04〜U-10 の一部に対応可能な骨格のみ。
- A-01〜A-08 に対応する管理画面機能は未実装。

## 3. 作業2 関連ファイル調査

### 3.1 モジュール依存と責務

依存関係:

- `main.py` -> `api.py` -> `orchestrator.py` -> 各service -> `store.py`
- `schemas.py` は `api.py` 入出力専用
- `models.py` は `store.py` と `service` 群で共有

責務の評価:

- レイヤー分離は概ね良好（API / Service / Store）。
- ただし管理画面、分析、外部導線、認証拡張が不足。

### 3.2 既存設定流用可否

既存設定:

- `AppConfig` にシャッフル、履歴、課金商品が定義済み。
- 追加実装はこの設定拡張で対応し、同等パラメータの乱立を避ける。

### 3.3 業界標準との比較

比較結果:

- FastAPI バックエンドMVPとしては標準的。
- モバイル・管理画面・永続DB・通知運用・分析が欠けており、
  サブスク運用アプリの一般的な本番構成には未到達。

### 3.4 コードレベル原因

原因:

1. 初期段階でバックエンドMVP中心に実装されたため、全体スコープが未網羅。
2. 永続化層・管理画面層・モバイル層の実装ファイルが未作成。
3. 認証連携、通知配信運用、分析イベント設計の実装が不足。

## 4. 作業3 修正方針

### 4.1 実装方針（最小破壊・責務厳守）

方針:

1. 既存の API / Service / Store 構成を維持し、拡張で対応する。
2. 既存コンフィグを拡張し、ハードコードを避ける。
3. 画面ID対応を明示するため、モバイルと管理画面のシステムファイルを追加する。
4. API拡張が既存フローに影響しないよう、既存エンドポイントを壊さない。
5. JST前提、ログ日本語の方針を継続する。

### 4.2 変更対象ファイル

既存拡張:

- src/oracle_app/config.py
- src/oracle_app/models.py
- src/oracle_app/store.py
- src/oracle_app/schemas.py
- src/oracle_app/orchestrator.py
- src/oracle_app/api.py
- src/oracle_app/services/*.py

新規追加:

- src/oracle_app/services/auth_service.py
- src/oracle_app/services/admin_service.py
- src/oracle_app/services/analytics_service.py
- src/oracle_app/services/link_service.py
- tests の追加ケース
- scripts の補助スクリプト追加（必要時）
- mobile_app（Flutter構成ファイル群）
- admin_app（Next.js構成ファイル群）

### 4.3 影響範囲

API変更の影響:

- 認証拡張は課金・履歴・通知設定のユーザー特定に影響。
- 管理API追加はカード/デッキ/テーマ/告知/通知運用に影響。
- 分析イベント追加はKPI算出とダッシュボードに影響。

画面影響:

- U-03〜U-19 のAPI接続先が増加。
- A-01〜A-08 の管理画面が新規追加される。

### 4.4 リグレッション抑止

対策:

- 既存テスト6件を維持。
- 追加APIごとに新規テストを追加。
- 既存エンドポイントのリクエスト/レスポンス互換を維持。

## 5. 重要項目

本ユーザー依頼（「全てのシステムファイルを作成してください」）を、
作業4以降の実装承認として扱い、作業4〜7を連続実施する。
