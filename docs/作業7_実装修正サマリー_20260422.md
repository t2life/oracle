# リーディングアプリ 作業7 実装修正サマリー（全体実装ラウンド）

## 1. 実施日と対象

| 項目 | 内容 |
|---|---|
| 実施日 | 2026-04-22 |
| 対象フォルダ | C:\Users\81907\Desktop\オラクルカード\system |
| 実施範囲 | 作業4〜作業7 |
| 作業対象外 | backup |

## 2. 実装結果（バックエンド）

### 2.1 配線と責務拡張

- `src/oracle_app/orchestrator.py`
  - `AuthService` / `AdminService` / `AnalyticsService` / `LinkService` をDI配線。
- `src/oracle_app/services/__init__.py`
  - 新サービスのexportを追加。
- `src/oracle_app/store.py`
  - 管理向け一覧取得を追加。
  - `list_all_themes` / `list_all_decks` / `list_all_announcements` / `list_admin_users` を実装。

### 2.2 APIスキーマ拡張

- `src/oracle_app/schemas.py`
  - Apple/Google認証、カード一覧、商品一覧、購入復元、分析イベント、管理ログイン、
    管理ダッシュボード、管理ユーザー、通知キャンペーン、外部導線、KPIなどを追加。

### 2.3 APIエンドポイント拡張

- `src/oracle_app/api.py` を全面更新。
- 既存互換を維持しつつ、以下を追加。

認証:
- `POST /auth/apple`
- `POST /auth/google`

コンテンツ:
- `GET /cards`
- `GET /products`

課金:
- `POST /purchase/restore`

外部導線:
- `GET /links/shop`
- `GET /links/consultation`
- `GET /links/live`
- `GET /links/live-events`

分析:
- `POST /analytics/events`
- `GET /admin/analytics/kpi`

管理:
- `POST /admin/login`
- `GET /admin/dashboard`
- `GET /admin/users`
- `GET /admin/inquiries`
- `GET /admin/announcements`
- `POST /admin/announcements`
- `GET /admin/themes`
- `GET /admin/decks`
- `GET /admin/admin-users`
- `GET /admin/campaigns`
- `POST /admin/campaigns`
- `POST /admin/campaigns/{campaign_id}/dispatch`

### 2.4 分析イベント連携

- 占いフローと課金フローでKPIイベントを記録するよう拡張。
  - 例: `theme_selected`, `shuffle_started`, `pile_selected`, `card_selected`,
    `reading_completed`, `history_viewed`, `ticket_purchased`, `subscription_started`。

## 3. 実装結果（モバイル層）

新規ディレクトリ:
- `mobile_app`

主な追加:
- `mobile_app/pubspec.yaml`
- `mobile_app/lib/core/routing/app_router.dart`
- `mobile_app/lib/core/network/api_client.dart`
- `mobile_app/lib/core/state/app_state.dart`
- `mobile_app/lib/core/state/app_state_scope.dart`
- `mobile_app/lib/features/**`

画面ID対応（実API接続を優先着手した画面）:
- U-01/U-02 オンボーディング
- U-03 ホーム（実データ表示 + 再取得）
- U-04 デッキ選択（実API取得デッキを状態管理へ反映）
- U-05 テーマ選択（実API取得テーマを状態管理へ反映）
- U-06 シャッフル（start/complete-shuffle API接続）
- U-07 山選択（select-pile API接続）
- U-08 カード展開（select-card API接続）
- U-09 結果表示（結果表示 + save-history API接続）
- U-10 履歴（history API接続）
- U-12 カード一覧
- U-13 お知らせ（announcements API接続）
- U-14 ロンの部屋
- U-15 本格鑑定導線
- U-16 ショップ導線
- U-17 マイページ（購入復元/通知トークン同期 API接続）
- U-18 課金画面（products表示 + restore API接続）
- U-19 お問い合わせ（inquiries API接続）

備考:
- ルーティングをU-19問い合わせ画面へ整合させた。
- U-12/U-14/U-15/U-16 はプレースホルダを廃止し、実API連携画面へ置換した。

## 4. 実装結果（管理画面層）

新規ディレクトリ:
- `admin_app`

主な追加:
- `admin_app/package.json`
- `admin_app/lib/api.ts`
- `admin_app/components/nav-shell.tsx`
- `admin_app/app/**`

画面ID対応（骨格）:
- A-01 管理ログイン
- A-02 ダッシュボード
- A-03 テーマ/デッキ/カード管理
- A-04 お知らせ管理
- A-05 通知キャンペーン管理
- A-06 ユーザー管理
- A-07 問い合わせ管理
- A-08 分析管理

## 5. テスト結果

実行コマンド:
- `c:/Users/81907/Desktop/オラクルカード/system/.venv/Scripts/python.exe -m pytest -q`
- `c:/Users/81907/Desktop/オラクルカード/system/.venv/Scripts/python.exe scripts/smoke_test.py`
- `c:/Users/81907/Desktop/オラクルカード/system/.venv/Scripts/python.exe scripts/init_persistence.py`

結果:
- pytest: `16 passed`
- smoke_test: 正常完了（結果JSON出力を確認）
- init_persistence: 既定設定（InMemory）で正常完了
- init_persistence: `staging + inmemory` で想定通り起動拒否（ガード有効）

追加確認:
- `admin_app` の `npm audit` high/critical を 0 件化済み（前工程で実施）。

## 6. 変更影響と整合

- 既存の占いMVP APIは互換を維持。
- 新規APIは管理・分析・導線・認証拡張を追加。
- 仕様書の全体スコープに向け、バックエンド以外のシステムファイル（モバイル/管理）を新規作成。

## 7. 未完了事項（次ラウンド）

- PostgreSQL/Redis 実機環境で migration up/down の本番同等検証が未完。
- `staging` / `production` 相当の実接続（hybrid）起動確認は未完。
- 監査ログの保持期間・マスキング方針は運用設計への落とし込みが未完。

## 8. PostgreSQL / Redis 移行着手の実装内容

追加ファイル:
- `src/oracle_app/persistence/postgres.py`
- `src/oracle_app/persistence/redis_cache.py`
- `src/oracle_app/persistence/hybrid_store.py`
- `src/oracle_app/persistence/__init__.py`
- `scripts/init_persistence.py`
- `scripts/migrations/001_expand_persistence_up.sql`
- `scripts/migrations/001_expand_persistence_down.sql`
- `scripts/run_migration.py`
- `.github/workflows/backend-ci.yml`

更新ファイル:
- `src/oracle_app/config.py`
  - 永続化用設定（`ORACLE_PERSISTENCE_BACKEND`, `ORACLE_POSTGRES_*`, `ORACLE_REDIS_*`）を追加。
  - 実行モード設定（`ORACLE_RUNTIME_MODE`）を追加。
- `src/oracle_app/orchestrator.py`
  - 永続化有効時はHybridStoreを起動。
  - `staging/production` では `inmemory` を禁止し、初期化失敗時の自動フォールバックを禁止。
- `requirements.txt`
  - `sqlalchemy`, `psycopg[binary]`, `redis` を追加。
- `src/oracle_app/api.py`
  - `/health/ready`, `/auth/logout`, `/admin/audit-logs` を追加。
  - 認証/課金/管理操作への監査ログ記録を追加。

段階導入の挙動:
- 既定値はInMemory維持（既存テスト互換）。
- 永続化設定を有効化した場合、結果・履歴を中心にPostgreSQLへ段階反映。
- Redisは結果/履歴の短期キャッシュとして併用。
