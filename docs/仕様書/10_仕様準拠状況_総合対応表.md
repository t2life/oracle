# 仕様準拠状況 総合対応表（2026-07-02 更新版）

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | 仕様準拠状況 総合対応表（更新版） |
| 版数 | Ver.2.0 |
| 作成日 | 2026-07-02 |
| 更新元 | `docs/仕様書対実装_対応表_20260422.md`（Ver.1.0） |
| 更新理由 | 旧対応表は2026-04-22時点の監査であり、その後の是正ラウンド（作業4-7等）で**モバイルアプリ・管理画面・永続化拡張が新規実装された**ため、多数の「未実装」判定が現状と乖離している。本書は実ファイル確認に基づき判定を更新する |

## 2. 【重要】旧対応表からの主要な訂正

| 項目 | 旧対応表（20260422）の判定 | 現状の判定 | 訂正根拠 |
|---|---|---|---|
| モバイルアプリ | 実装ファイルなし／未実装 | **実装済み**（18画面、mobile_app/lib配下31ファイル） | `mobile_app/lib/features/**/*_screen.dart`の実在確認 |
| 管理画面 | 実装ファイルなし／未実装 | **実装済み**（10ページ、admin_app配下） | `admin_app/app/**/page.tsx`の実在確認 |
| U-12/U-14/U-15/U-16 | PlaceholderScaffoldのまま未接続 | **本実装済み**（実API接続） | `作業4-7_次ラウンド是正実装と検証結果_20260422.md`、実コード確認 |
| DB/キャッシュ | InMemoryStoreのみ | **部分的に永続化済み**（users/reading_results/reading_history/announcements/inquiries/audit_logsはPostgres+Redis対応のHybridStore経由） | `src/oracle_app/persistence/hybrid_store.py`実装確認 |
| CI/CD | 記載なし（未評価） | **実装済み**（GitHub Actions、Postgres/Redisサービスコンテナ利用） | `.github/workflows/backend-ci.yml`実在確認 |
| 監査ログ | 記載なし（未評価） | **実装済み**（認証・課金・管理操作の主要導線に記録、参照API `/admin/audit-logs`あり） | `api.py`の`_audit_action`呼び出し箇所確認 |

## 3. レイヤー別対応表（更新版）

| レイヤー | 仕様要求 | 現状実装 | 判定（旧→新） |
|---|---|---|---|
| モバイルアプリ | iOS/Android本体実装 | Flutter実装18画面、全画面API接続済み | 未実装 → **実装済み** |
| バックエンドAPI | 認証/占い/履歴/課金/通知/告知/問合せ | 32エンドポイント実装（`05_API仕様書.md`） | 部分実装 → **実装済み**（外部決済/通知配信は未接続） |
| 管理画面 | A-01〜A-08 | 10ページ実装（`04_詳細設計書_管理画面.md`） | 未実装 → **実装済み**（権限ロール制御・マスタ編集APIは未実装） |
| DB/キャッシュ | PostgreSQL/Redis | HybridStore経由で6ドメイン永続化済み、他はInMemory | 未実装 → **部分実装** |
| 通知運用 | 即時/予約/セグメント/AB/効果測定 | 作成・一覧・ディスパッチAPIあり、実配信（FCM/APNs）は未接続 | 未実装 → **部分実装** |
| 分析基盤 | KPIとイベント計測 | `analytics_events`記録＋KPIスナップショットAPI実装済み（InMemory） | 未実装 → **部分実装** |
| 外部導線 | ショップ/鑑定/VTuber | API・モバイル画面とも実装済み（URLはダミー値） | 未実装 → **実装済み**（実URL未設定） |

## 4. API要件対応（詳細仕様書18章、更新版）

| 仕様API | 実装状況 | 変化 |
|---|---|---|
| POST /auth/login | 実装済み | 変化なし |
| POST /auth/apple | **実装済み** | 未実装 → 実装済み |
| POST /auth/google | **実装済み** | 未実装 → 実装済み |
| GET /themes, /decks | 実装済み | 変化なし |
| POST /reading/start〜GET /reading/{id} | 実装済み | 変化なし |
| GET /history | 実装済み | 変化なし |
| POST /purchase/verify | 実装済み（外部レシート検証は未接続） | 変化なし |
| POST /purchase/restore | **実装済み** | 未実装 → 実装済み |
| POST /notifications/token | 実装済み | 変化なし |
| GET /announcements | 実装済み | 変化なし |
| POST /inquiries | 実装済み | 変化なし |
| GET /cards, /products | **実装済み**（新規） | 未評価 → 実装済み |
| GET /links/shop, /links/consultation, /links/live, /links/live-events | **実装済み**（新規） | 未評価 → 実装済み |
| POST /analytics/events | **実装済み**（新規） | 未評価 → 実装済み |
| GET /health/ready, POST /auth/logout | **実装済み**（新規） | 未評価 → 実装済み |
| /admin/* 全14種 | **実装済み**（新規） | 未実装 → 実装済み |

## 5. 画面IDトレーサビリティ（更新版）

| 画面ID群 | 旧判定 | 新判定 | 根拠 |
|---|---|---|---|
| U-01〜U-03 | UI実装なし | **実装済み** | `mobile_app/lib/features/{onboarding,home}` |
| U-04〜U-10 | 部分実装（APIのみ） | **実装済み**（UI＋API接続） | `07_UI画面遷移_ワイヤーフレーム仕様書.md`§3 |
| U-11 | 未実装 | **要確認**（独立画面の実在未確認） | 同上 |
| U-12〜U-19 | 未実装〜部分実装 | **実装済み**（U-12/14/15/16は是正済み） | 同上 |
| A-01〜A-08 | 未実装 | **実装済み**（権限ロール制御は未実装） | `04_詳細設計書_管理画面.md`§4 |

## 6. データモデル対応（詳細仕様書17章、更新版）

詳細は`06_データベース設計書.md`§6の対応表を正とする。要約:

- **Postgres永続化済み**: users, announcements, inquiries, audit_logs, reading_results, history_items
- **InMemoryのみ（部分実装）**: profiles, auth_accounts, decks/cards/themes, subscriptions/ticket_wallets（テーブル定義のみで実書込未接続）, push_campaigns, notification_settings, products, consultation_links/live_events, admin_users/admin_roles, analytics_events
- **未実装**: spreads（独立モデルなし）, ticket_transactions

## 7. テスト観点（更新版）

旧対応表は6件のテストのみを記録していたが、現状は**16件**（`08_テストケース仕様書.md`参照）。追加された10件は、Apple/Google認証、管理API、監査ログ、永続化ブートストラップ（HybridStore）を対象とする。

## 8. 残る未充足領域（総括・2026-08-31更新）

1. 外部決済レシート検証（Apple/Google公式API）との実接続。
   ※2026-08-31に検証器抽象化層（`ReceiptVerifier`・モード切替・production起動ガード）を実装済み。
   残作業は認証情報投入と実HTTP呼び出しの実装（B-1/B-2）。
2. プッシュ通知の実配信（FCM/APNs）との実接続。
   ※2026-08-31に送信器抽象化層（`PushSender`・noop明示・fail-closed）を実装済み。
   残作業はFCM HTTP v1実呼び出しの実装（B-3）。
3. ~~管理者ロール別アクセス制御の実装。~~ → **実装済み（2026-08-31）**。4ロール＋エンドポイント別権限照合
   （`05_API仕様書.md`§11参照）。管理者アカウントのCRUD APIは未実装（ロール変更はシード定義で管理）。
4. コンテンツマスタ（テーマ/デッキ/カード/商品/外部リンク）のPostgres化と管理画面からの編集反映。
5. 分析イベントのPostgres化（KPIの永続的な蓄積）。
6. 法務文書の正式化: `09_プライバシーポリシー.md`に加え、`14_特定商取引法に基づく表記.md`・
   `15_利用規約.md`のドラフトを2026-08-31に新規作成。いずれも事業者情報の確定と法務レビューが残作業。
7. モバイル/管理画面の自動テスト（Widgetテスト・E2Eテスト）整備。
8. 実PostgreSQL/Redis環境でのステージング相当運用検証。
9. 外部URL（ショップ/鑑定/ライブ）の実URL設定（2026-08-31時点でオーナー判断は保留。ダミーURL継続）。
10. U-06シャッフルのLottie演出実装（方針は確定済み・`03_詳細設計書_モバイルアプリ.md`§6.1。Flutterビルド検証環境の確保後に実施）。

## 9. 結論

現時点のシステムは、2026-04-22時点の是正指摘書が要求した「モバイルUI・管理画面・永続DB・通知運用・分析・外部導線」のうち、**モバイルUI・管理画面・外部導線・分析基盤の骨格は実装済み**となった。残る主要ギャップは、**外部サービス（Apple/Google決済検証、FCM/APNs、コンテンツマスタの完全永続化）との実接続**、および**運用系（ロール制御・法務文書・自動テスト拡充）**に絞り込まれている。詳細な残作業は`11_今後の開発ロードマップ.md`を参照。
