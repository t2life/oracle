# テストケース仕様書

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | テストケース仕様書 |
| 版数 | Ver.1.0 |
| 作成日 | 2026-07-02 |
| 実行コマンド | `pytest -q`（PYTHONPATH=src） |
| 現状件数 | 16件（全PASS） |

## 2. テストファイル別 観点マトリクス

### 2.1 test_api_reading_flow.py（3件）

| テスト関数 | 検証観点 |
|---|---|
| `test_full_reading_flow_and_history_save` | セッション開始→シャッフル完了→山選択→カード選択→履歴保存→結果取得→履歴一覧取得の一連が200で完了し、履歴が1件登録されること |
| `test_free_plan_daily_limit` | 無料ユーザーが同日2回目のセッション開始を試みると403で拒否され、メッセージに「1日1回」を含むこと |
| `test_shuffle_rule_validation` | `idle_seconds=0.5`（閾値1.0未満）でシャッフル完了を試みると400で拒否されること（シャッフル終了判定の下限検証） |

### 2.2 test_billing_and_limits.py（3件）

| テスト関数 | 検証観点 |
|---|---|
| `test_subscription_unlocks_three_card_draw` | サブスク会員は`draw_count=3`のセッションを開始できること |
| `test_ticket_plan_consumes_tickets` | チケットプランのユーザーがセッション開始時に所持チケットを消費すること |
| `test_free_history_keeps_latest_three` | 無料ユーザーの履歴は直近3件のみ保持され、古いものが切り捨てられること |

### 2.3 test_extended_api.py（6件）

| テスト関数 | 検証観点 |
|---|---|
| `test_provider_login_and_catalog_endpoints` | Apple/Googleログイン、カード検索、商品一覧、外部リンク取得の一連が正常応答すること |
| `test_purchase_restore_and_analytics_event` | 購入復元APIと分析イベント記録APIが正常応答すること |
| `test_admin_operations_endpoints` | 管理API（ログイン・ダッシュボード・お知らせ作成・キャンペーン作成/配信等）が正しいトークンで動作すること |
| `test_admin_endpoint_requires_token` | 管理APIがトークン無しで401を返すこと |
| `test_health_ready_logout_and_admin_audit_logs` | `/health/ready`・`/auth/logout`・`/admin/audit-logs`の正常応答と監査ログaction（`auth.login`, `auth.logout`, `billing.purchase_restore`等）の記録確認 |
| `test_admin_audit_logs_requires_token` | `/admin/audit-logs`がトークン無しで401を返すこと |

### 2.4 test_persistence_bootstrap.py（4件）

| テスト関数 | 検証観点 |
|---|---|
| `test_app_config_reads_persistence_env` | `ORACLE_RUNTIME_MODE`・`ORACLE_PERSISTENCE_BACKEND`等の環境変数がAppConfigへ正しく反映されること |
| `test_build_store_rejects_inmemory_in_staging` | `staging`モードで`inmemory`指定した場合に起動が拒否されること（ガード動作確認） |
| `test_hybrid_store_dual_write_and_rehydrate_from_postgres` | HybridStoreがPostgresへ書込み、InMemoryキャッシュ消失後もPostgresから再水和（rehydrate）できること |
| `test_hybrid_store_history_cache_and_trim_propagation` | 履歴のRedisキャッシュ無効化とtrim（件数制限）がPostgres/Redis双方に伝播すること |

## 3. スモークテスト・補助スクリプト

| スクリプト | 検証内容 |
|---|---|
| `scripts/smoke_test.py` | アプリ起動後、`/health`, `/health/ready`, `/auth/login`, `/auth/logout`, `/admin/audit-logs`等の主要導線が疎通することを確認 |
| `scripts/init_persistence.py` | 永続化バックエンドの初期化可否を確認（`local+inmemory`成功、`staging+inmemory`失敗が期待動作） |
| `scripts/run_migration.py up/down` | マイグレーションSQLの適用・ロールバック（DB未接続時はタイムアウトで失敗するのが期待動作） |

## 4. 詳細仕様書とのテスト対応表

| 仕様要件 | 対応テスト | カバー状況 |
|---|---|---|
| 無料1日1回制限（11.1章） | `test_free_plan_daily_limit` | カバー |
| シャッフル終了判定（9.6章） | `test_shuffle_rule_validation` | 下限のみカバー（上限2.0秒超過・finger_released=false・swipe_distance不足の個別ケースは未カバー） |
| 3枚引き解放（11.3章） | `test_subscription_unlocks_three_card_draw` | カバー |
| チケット消費（11.2章） | `test_ticket_plan_consumes_tickets` | カバー |
| 履歴無料3件制限（9.10章） | `test_free_history_keeps_latest_three` | カバー |
| Apple/Google認証（10.2章） | `test_provider_login_and_catalog_endpoints` | 正常系のみカバー（異常系・重複アカウント処理は未カバー） |
| 購入復元（11.4章） | `test_purchase_restore_and_analytics_event` | カバー |
| 管理画面認可（14.1章） | `test_admin_endpoint_requires_token`, `test_admin_audit_logs_requires_token` | トークン欠如の401判定のみカバー（ロール別権限は未カバー・そもそも未実装） |
| 監査ログ（23.3章 非機能） | `test_health_ready_logout_and_admin_audit_logs` | カバー |
| 永続化本番運用ガード（20章） | `test_build_store_rejects_inmemory_in_staging` | カバー |
| HybridStoreの整合性 | `test_hybrid_store_dual_write_and_rehydrate_from_postgres`, `test_hybrid_store_history_cache_and_trim_propagation` | カバー（ユニットレベル。実PostgreSQL/Redis接続でのE2E検証は未実施） |

## 5. 未カバー主要領域

1. モバイルアプリのWidgetテスト・統合テスト（`flutter test`は未整備）。
2. 管理画面（Next.js）のコンポーネントテスト・E2Eテスト。
3. 実際のApple/Google OAuthプロバイダとの結合テスト（現状は`provider_user_id`を直接渡すモック相当のテストのみ）。
4. ストア公式レシート検証APIとの結合テスト（Sandbox環境での実地検証）。
5. 通知配信の実FCM/APNs送信結果の確認。
6. 実PostgreSQL/Redis環境でのステージング相当負荷・障害注入テスト。
7. 課金の失効・猶予期間・二重課金防止の網羅的境界値テスト。

## 6. 受入試験マトリクス（開発会社向け是正要求パッケージ 7章 準拠）

| 項目 | 受入条件 | 現状 |
|---|---|---|
| U-03〜U-10画面遷移 | 実機/エミュレータで遷移完了 | 未実施（コード実装のみ確認、実機検証は`11_今後の開発ロードマップ.md`のタスク） |
| Apple/Google認証 | ステージングでログイン成功・会員状態作成 | 未実施（ローカルテストのみ） |
| 履歴永続化 | サーバ再起動後も履歴保持 | `test_hybrid_store_*`でユニットレベル確認済み。実運用環境での再起動試験は未実施 |
| 課金状態永続化 | 再起動後も権限状態保持 | 部分未達（`users`テーブルは永続化済みだが`subscriptions`/`ticket_wallets`専用テーブルへの書込は未接続。`06_データベース設計書.md`参照） |
| 通知予約配信 | 指定時刻配信 | 未実施（配信先送信の実連携が未接続） |
| 課金検証 | Sandboxレシートで冪等性確認 | ユニットレベルのみ（`receipt_id`重複防止ロジック）。実レシートでの検証は未実施 |
