<!--
本書はこのリポジトリの作業ルールである。**単一の真実源はこのファイル**とし、
リポジトリの外に写しを置かないこと（写しは必ず古くなる）。

パスはすべて**リポジトリのルート（本書のある階層）からの相対**で書く。
Windows / macOS のどちらで作業してもそのまま読めるようにするためであり、
絶対パスや利用者名を書き足さないこと（本リポジトリは公開である）。

iOS 移植の作業者は、あわせて `docs/引き継ぎ_iOS移植_20260912.md` を読むこと。
-->

`docs/`（本書と同じ階層）に前回作業がある場合は、

前回と今回の調査内容・修正内容・不足機能を正確に把握し、

「リーディングアプリ開発 詳細仕様書」「リーディングアプリ UI設計フェーズ資料」および

必要に応じて追補資料に準拠して、品質ガイドやモバイルアプリの業界標準に照らして修正案を作成してください。

その修正が及ぼす他のシステムファイル、画面、API、管理画面、通知、課金、履歴、分析、外部導線への影響も熟考し、

十分に検証した上でコーディングしてください。

承認なしに修正はしません。作業1～3までは承認なしで進みます。

場合によっては作業1～7まで進みます。

作業3の下部にある重要項目の承認可否を確認して進めてください。

また、作業に必要なファイルの読み取りと書き込みは許可します。

■作業1: 事実確認

・作業対象フォルダ: 本書のある階層（リポジトリのルート）配下すべて

・ドキュメント保存フォルダ: `docs/`

・作業非対象フォルダ: backup

・GitHubリポジトリ: <https://github.com/t2life/oracle>

・必ず READ モードで既存システムファイルを取得し、まず ripgrep 等で高速検索しながら TODO リストを作成する。

・仕様書、追補仕様書、UI設計資料、前回作業ファイル、README、workspace設定、既存実装コードを対象に事実確認する。

・ログファイルが存在する場合は、時系列で横断確認して問題要因を把握する。

前提認識:

・本アプリは「占い体験」単体ではなく、モバイルアプリ、バックエンドAPI、管理画面、通知、課金、履歴、分析、ショップ導線、鑑定導線、ロンの部屋導線を含むシステム全体として扱う。

・対象は金融システムではない。EA、売買ロジック、最新金融理論の観点は適用しない。

やること:

・/docs の md, docx, xlsx を確認し、問題の背景と仕様の基準を把握する。

・問題を把握したうえで、/docs の md ファイル、該当ソースコード、既存ファイル、クラス、関数、変数、設定値、パラメータを READ モードで完全調査する。

・必要ファイルが足りなければ、ユーザーに提供を依頼する。

確認項目:

・仕様書に定義された対象範囲（モバイルアプリ、API、管理画面、通知、課金、履歴、分析、外部導線）との対応。

・修正対象ファイルの全クラス定義。

・全関数・メソッド定義とその配線（どこから呼ばれているか）。

・全 import 文とその配線。

・データモデル、schema、DTO、dataclass 等の使用状況。

・画面ID（U-xx / A-xx 等）と実装ファイルの対応状況。

・承認を待たずに作業2へ進んでよい。

■作業2: 関連ファイルの調査（まだ修正コーディングはしない）

やること:

・修正対象に関連するファイル・モジュールを READ モードで調査する（まだコード変更はしない）。

確認項目:

・関連関数・メソッド定義の存在と配線。

・関連 import 文の存在と配線。

・全呼び出し経路の確認。

・既存設定を無視していないか、既存設定内に類似パラメータがないか、流用可能な設定がないか（パラメータ乱立防止）。

・モジュール間の依存関係。

・既存の命名規則。

・モバイルアプリ業界標準、占い・診断・サブスクアプリの一般的設計、FastAPI / Flutter / Next.js 等の標準構成と比較した位置付け。

・ファイル間のオーケストレーションと各ファイルの責務。

・コードレベルで問題の原因を特定する。

・仕様書の章番号、機能一覧、画面一覧、API一覧との乖離を整理する。

・承認を待たずに作業3へ進んでよい。

■作業3: 修正方針（または調査回答）の策定（まだ修正コーディングはしない）

結果は /docs フォルダに md ファイルとして保存する（markdownlint ルール遵守）。

ファイル間のオーケストレーションと責務を厳守すること。

やること:

・問題の原因をコードレベルで明確に提示する。

・各ファイルのオーケストレーション・責務を守る前提で方針を書く。

・冗長化を招く回避策は採用しない。根本原因を特定し、シンプルなコーディング方針を示す。

・冗長化やシステム不安定を招く安易なコード追加を禁止する。常にシステム全体のオーケストレーションと責務を認識する。

・新規パラメータを追加する場合は、類似属性・既存設定・既存パラメータで代用または拡張できないかを確認する。

・どのファイルにどのような変更を加えるかを明記する。

・オーバーライドによる一時逃げは禁止。既存オーバーライドがある場合は整理・統合し、関連ファイルも必要に応じて修正対象に含める。

・命名・配置が混在していたら統合方針を示す。

・既存コードをリグレッションしないよう配慮する。

・モバイルアプリ、サブスクアプリ、通知運用、CMS管理画面の業界標準と比較・評価し、必要なら取り入れる。

・新規クラス/関数追加ではなく、既存クラス/関数の拡張で対応できないか検討する。

・ハードコーディングを避け、既存コンフィグやマスタ管理を使う。

・最小変更で実現できるか（保守性・可読性・効率の観点）を検討する。

・補助関数等、既存の再利用可能な関数を使ってコーディング量を削減できないか検討する。

・時刻関係のコードは JST を前提に扱い、保存時のタイムゾーン整合も考慮する。

・必要であれば debug レベルログを追加する。

・ログ出力は日本語で記述する。

・UI設計資料の画面IDと実装対象の対応を明記する。

・API変更が画面、課金、通知、履歴、管理画面へ与える影響も明記する。

■■重要: 承認を待って作業4へ進む。

■作業4: 実装（backup フォルダ以外）

注意: backup フォルダは修正対象外。既存機能を絶対に壊さないこと（リグレッション防止）。

前提:

/docs にある作業1〜3で作成した md ファイル、

および「リーディングアプリ開発 詳細仕様書」「リーディングアプリ UI設計フェーズ資料」を READ モードで取得し、

仕様・方針どおりに実装する。

守るべきルール:

・オーケストレーション違反とファイルの責務を厳守する。

・モバイルアプリ、通知、課金、CMS、分析、API設計の業界標準を積極的に取り入れる。

・既存コードを調査し、重複・類似コードは追加しない。また、重要な処理を削除・省略しない。

・修正によって不要になった重複メソッド・コードは削除する。

・テストを通すために重要な機能を壊さない。

・明示された仕様・方針を絶対基準とし、「仕様とは違うがより良さそう」な案はまず提案し、承認を得るまで実装しない。

・仕様テキストの各行、画面要件、API要件、権限制御要件と実装を対応づける。

・仕様と異なる振る舞いは混ぜない。例外が必要な場合は、明示的フラグ・コメント・ログで仕様外と分かる形に限定する。

・システムに存在しないテスト専用パラメータを本体実装に入れない。

・READ した既存コードを基準に修正する。

・コード出力は、修正した関数・メソッド・クラス単位、または前後関係が分かるブロック単位で行う。

・インデント、条件分岐、非同期処理、例外処理の位置関係に注意する。

・承認を待たずに作業5へ進んでよい。

■作業5: 配線確認とテスト実施

やること:

・関連ファイルとの配線（import、呼び出し関係、configやmasterとの連携）を確認する。

・作業3・4の注意事項を考慮したテストを実施する。

・テストファイルは tests フォルダに作成する。

・補助スクリプトは scripts フォルダに作成する。

・仮想環境は下記で有効化する。

　⇒ Windows: `.venv\Scripts\Activate.ps1`　／　macOS: `source .venv/bin/activate`

　（いずれもリポジトリのルートからの相対。Mac で Flutter だけを触る場合は不要）

テスト観点:

・占いフロー（デッキ選択、テーマ選択、シャッフル、山選択、カード確定、結果表示）

・無料／有料権限制御

・履歴保存／履歴制限

・通知登録／お知らせ取得

・課金復元／購入状態反映

・外部導線のリンク整合

・主要APIとUIの整合

・承認を待たずに作業6へ進んでよい。

■作業6: 最終チェック（重要）

チェック項目:

・構文・文法の正しさ。

・インデント位置、条件分岐、例外処理の妥当性。

・類似機能との競合。

・重複や、必要な既存機能の欠落・誤削除がないか。

・リグレッションの有無。

・ハードコーディングの有無。

・オーケストレーション違反がないか。

・配線（import・呼び出し）の整合性。

・必要なロギングがあるか、日本語で出ているか。

・仕様書、UI設計資料、前回修正方針との不整合がないか。

・画面ID・API・モデル・設定値の命名一貫性が保たれているか。

・承認を待たずに作業7へ進んでよい。

■作業7: 実装・修正サマリーの報告

/docs フォルダに、今回の実装・修正サマリーの md ファイルを保存する（markdownlint ルール遵守）。

報告内容:

・どのように修正したか（ファイル単位・ロジック単位・画面/API単位）。

・どのような改善が得られたか（挙動・リスク・保守性・業界標準との比較）。

・仕様書のどの章・機能・画面ID・APIに対応したか。

・未対応事項と今後の実装対象。

・GitHub が存在しない場合は push / branch 作成を要求しない。

・backup, logs, user\_data, 資料, \_\_pycache\_\_ フォルダは作業対象外。

・これで完了。

■禁止事項（再確認）

・既存コードの確認なしに新規追加を提案しない。

・サマリーや検索結果だけで判断しない。

・「おそらく」「たぶん」に基づく提案をしない。

・承認なしで主要ステップ、特に実装開始へ進まない。

・複数ステップを一括で飛ばさない。

・既存コードの削除・無視でシステムを崩壊させない。

・金融システム向けの比較軸（EA、売買戦略、最新金融理論）を流用しない。

・ Git 前提、GitHub push 前提で存在しない運用を勝手に仮定しない。

■必ず行うこと

・READ モードでの完全なコード取得。

・仕様書、UI設計資料、追補資料、前回作業ファイルとの比較評価。

・原文コードベースでのコーディング。

・変数名、画面ID、API名、モデル名、設定名の命名一貫性チェック。

・不明点や不足ファイルは必ずユーザーに確認・要求する。

・既存実装との整合性確認。

・最小変更での実現可能性検討。

・各ステップでの承認待機（特に作業3→4の移行時）。

・変更理由の明確な説明。
